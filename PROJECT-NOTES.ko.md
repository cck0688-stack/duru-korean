# 두루한국어 프로젝트 현황

작성일: 2026년 9월 16일
사이트: <https://www.durukorean.com>

이 문서는 **인수인계 기록**입니다. 새 대화를 시작하면 AI는 이전 대화를
기억하지 못하므로, 이 문서와 `README.md`를 읽고 이어서 작업합니다.
사람이 이어받을 때도 마찬가지입니다.

---

## 1. 전체 구조

```
GitHub 저장소  ──push──▶  Vercel  ──▶  www.durukorean.com
 (코드 보관)              (배포)        (실제 사이트)
                                            │
                                            ▼
                                        Supabase
                              (회원·로그인·블로그·자료 파일)
```

| 구성 요소 | 내용 |
|---|---|
| 도메인 | `durukorean.com` (가비아 구매) · `www` 가 정식 주소, apex는 자동 이동 |
| 호스팅 | Vercel (무료 Hobby) |
| 코드 | GitHub `cck0688-stack/duru-korean` |
| 백엔드 | Supabase 프로젝트 `ejiwgvlinlffkyycuyym` |
| 기술 | 순수 HTML·CSS·JavaScript. 빌드 과정 없음 |

**빌드 과정이 없다**는 건, 파일을 고쳐서 올리면 그게 그대로 사이트가
된다는 뜻입니다. 복잡한 도구가 끼어 있지 않습니다.

---

## 2. 작업 흐름 (2026-09-16 변경)

브랜치가 두 개입니다.

| 브랜치 | 역할 |
|---|---|
| `claude/duru-korean-homepage-raysf6` | **실제 사이트.** 승인된 것만 들어감 |
| `preview` | **미리보기.** 새 작업이 먼저 올라감 |

```
요청 → 수정 → preview 푸시 → 미리보기 확인 → 승인 → 실제 사이트 반영
```

미리보기 주소: `https://duru-korean-git-preview-cck6.vercel.app`

> ⚠️ **미리보기도 실제 데이터베이스를 씁니다.** 미리보기에서 글을 쓰거나
> 지우면 진짜로 반영됩니다. 화면 확인용이지 연습장이 아닙니다.

---

## 3. 대표님이 직접 하실 수 있는 일

**개발자 없이 사이트 안에서** 하는 일들입니다. 로그인만 하면 됩니다.

### 블로그 글쓰기
`www.durukorean.com/blog.html` → **`+ 글쓰기`**

- 제목 · 분류 · 요약 · 본문 · **작성 언어**
- **"바로 공개하기"** 체크 해제 → 임시저장 (학생들에게 안 보임)
- **수정 / 삭제 / 게시** 는 글을 연 화면 위쪽 띠에서
- 본문은 **엔터 두 번**이 새 문단

### 블로그 자동 번역 (문장 단위)

한글로 쓰면 저장할 때 나머지 7개 언어로 **문장마다** 번역됩니다.
중국어를 선택한 독자는 한국어 문장 바로 밑에 중국어 문장을 봅니다.

- 저장 화면의 **"저장할 때 다른 언어로 번역하기"** 체크(기본 켜짐)
- 글을 열면 위쪽 띠에 **몇 개 언어로 번역됐는지** 표시되고,
  본문을 고치면 **"번역 다시 하기"** 버튼이 나옵니다
- 손으로 직접 번역해 넣은 언어는 자동 번역이 건드리지 않습니다
- 번역은 **한 번만** 돌고 저장됩니다. 독자가 읽을 때마다 비용이
  드는 구조가 아닙니다

**번역 회사는 골라서 쓸 수 있습니다.** 특정 회사에 묶여 있지 않아서,
Vercel 환경변수에 아래 중 **가지고 계신 키 하나만** 넣으면 그 회사로
번역합니다.

| 회사 | 환경변수 이름 | 가입처 |
|---|---|---|
| Anthropic (기본) | `ANTHROPIC_API_KEY` | console.anthropic.com |
| OpenAI | `OPENAI_API_KEY` | platform.openai.com |
| Google Gemini | `GOOGLE_API_KEY` | aistudio.google.com |
| DeepL | `DEEPL_API_KEY` | deepl.com/pro-api |

나중에 회사를 바꾸고 싶으면 **환경변수만 바꾸면 됩니다.** 코드는
그대로입니다. 키를 두 개 이상 넣었다면 `TRANSLATE_PROVIDER` 에
`anthropic` / `openai` / `google` / `deepl` 중 하나를 적어 고릅니다.

> 키가 하나도 없으면 나머지 사이트는 그대로 돌아가고, 번역 버튼만
> "번역이 아직 설정되지 않았다"고 답합니다. 설정법은 `README.md`의
> *Automatic translation, sentence by sentence* 참고.

### 자료 파일 올리기
`www.durukorean.com/free-resources.html` → 아래로 스크롤 →
**`+ Attach Resource`**

- PDF · PNG · JPG 20MB / MP3 · M4A 50MB
- 올리면 누구나(로그인 없이) 내려받을 수 있음

### 블로그 낱말 코너 (한글 글에만)

한글로 쓴 글 **바로 아래에** (공유 버튼보다 위) **"알아 둘 낱말"**
칸이 자동으로 붙습니다. 그 글에서
어려운 낱말 **5개**를 뽑아, 독자 언어로 뜻과 쓰임을 설명합니다.

- **문맥에 맞는 뜻**만 알려줍니다 — 김치 이야기의 "발효"는 음식이
  익는 뜻이지 법이 시행된다는 뜻이 아닙니다
- 낱말 5개는 **모든 언어가 같고**, 설명만 각 언어로 바뀝니다
- 번역할 때 같이 만들어집니다. 본문을 고치면 번역과 함께 다시
  만들어야 합니다 (**[다시 번역]** 버튼 하나로 둘 다)
- 한국어로 읽는 독자에게는 안 보입니다

> ⚠️ **"작성 언어"가 한국어여야 나옵니다.** 한글을 입력하면 이제
> 자동으로 한국어로 맞춰집니다. 이미 잘못 등록된 글은 글 위쪽 띠가
> *"영어로 등록돼 있지만 내용은 한국어입니다"* 라고 알려주니,
> Edit → 작성 언어를 한국어로 → 저장 → [다시 번역] 하시면 됩니다.

### 자기 언어로 보는 법을 알려주기

베트남 사람이 durukorean.com에 처음 오면 **영어 화면**을 받습니다. 자기
언어로 보려면 지구본을 알아보고, 눌러 보고, 목록에서 자기 언어를
찾아야 합니다 — **읽을 수 없는 화면에서**.

그럴 필요가 없습니다. 브라우저가 이미 자기 주인이 무슨 언어를 읽는지
말해 주고 있습니다(`navigator.languages`). 그걸 안 읽고 있었을 뿐입니다.

이제 화면 맨 위에 띠가 뜹니다:

```
文A  Trang này có bản tiếng Việt.  [ Xem bằng tiếng Việt ]  Stay in English  ✕
```

**이 문장이 방문자의 언어로 쓰인다는 게 핵심입니다.** 화면은 영어인데
띠만 베트남어입니다. 지구본이 뭔지 몰라도, 어디 있는지 몰라도 됩니다.
찾아오니까요.

지킨 세 가지:

1. **제안만 하고 바꾸지 않습니다.** 한국어 설정 폰을 쓰는 한국계
   미국인이 영어를 원할 수 있습니다. 몰래 바꾸면 그 선택을 뺏습니다.
2. **한 번만 뜹니다.** [보기]든 [계속]이든 [✕]든, 어떤 답이든 기억하고
   다시 안 뜹니다.
3. **답할 필요가 없는 사람에겐 안 뜹니다** — 이미 언어를 고른 사람,
   `?lang=` 링크로 온 사람, 우리가 지원 안 하는 언어, 이미 그 언어로
   보고 있는 사람.

> **IP나 국가 추측이 아닙니다.** IP는 추측이고(VPN·여행·교민),
> `navigator.languages`는 방문자가 자기 브라우저 설정에 **직접 적어
> 넣은 값**입니다. 기존 코드의 "국가 추측은 절대 쓰지 않는다"는 원칙은
> 그대로입니다.

브라우저가 `pt-PT`를 보내면 우리 `pt-BR`로, `zh-Hant-TW`면 `zh`로
연결합니다. 완벽히 같진 않지만 영어보다는 낫다는 판단입니다.

### 언어 선택

맨 위에서 언어를 고르면 **Free Downloads와 Blog의 언어 필터도 따라서**
그 언어로 맞춰집니다. 중국어를 고른 방문자는 두 목록 모두 중국어
자료·글이 먼저 보입니다.

블로그 목록 위의 **[읽을 언어]** 를 고르면 **페이지 전체가** 그 언어로
바뀝니다 — 맨 위 지구본을 누른 것과 똑같습니다. 예전에는 글 목록만
바뀌어서, 한국어 글이 영어 제목·영어 메뉴 아래에 놓이는 어정쩡한
상태가 됐습니다. "언어를 고르라"고 적혀 있으면 방문자는 페이지를
말하는 것이니, 이제 페이지가 바뀝니다.

자료 페이지의 **파일 언어**는 지금도 따로 움직입니다 — 그 언어로 된
파일이 없으면 *"이 자료는 아직 그 언어가 없습니다"* 라고 알려 주고
이전 파일을 유지합니다.

### 어떤 글을 쓰는가

가장 중요한 기준을 바꿨습니다. **"도움이 되는 글"에서 "한국 사람에겐
당연한데 외국인에겐 신기한 글"로.**

공항에서 시내 가는 법, 메뉴에 사진이 없을 때 주문하는 법 — 틀린 말은
아니지만 어느 가이드북에나 있고, 아무도 읽지 않습니다. 놀랄 것이 없기
때문입니다.

이제 로봇은 주제 후보마다 **"한국에서 자라지 않은 사람이 이걸 처음
알았을 때 얼마나 놀랄까"** 를 1~10점으로 매기고, **6점 미만이면 아예
고르지 않습니다.** 이 점수는 가중치도 두 배입니다.

이런 글을 찾습니다:

- 식당에 들어가면 묻지도 않았는데 반찬이 나오고, 더 달라고 해도 공짜다
- 카페에 노트북을 두고 화장실에 간다
- 처음 만난 사람이 나이를 묻는다 — 무례한 게 아니라 말투를 정하려는 것
- 이사할 때 집 전체가 사다리차로 창문을 통해 올라간다

실용적인 주제를 꼭 써야 할 때도 **놀라운 지점에서 시작**하게 했습니다.
"택시 잡는 법"은 뻔하지만 "한국 택시는 왜 빈 차에 빨간 불이 켜져 있는가"는
읽힙니다. 같은 정보가 들어가도 들어가는 문이 다릅니다.

본문 규칙에도 **"왜 그런지를 반드시 설명한다"** 를 넣었습니다. 무엇을
하는지만 나열한 글이 곧 가이드북입니다. 번호 목록은 한 덩어리에 5개를
넘기지 않습니다 — 일곱 가지를 늘어놓느니 두세 가지를 제대로 설명하는
편이 낫습니다.

### 글쓰는 방식 30가지

글마다 **쓰는 형식을 미리 정해 놓고** 씁니다. 예전에는 전부 같은 틀이라
— 사례 한 줄, 번호 세 개, 마무리 한 줄 — 하나하나는 괜찮아도 하루에
일곱 편을 보면 기계가 쓴 티가 났습니다.

서른 가지가 서로 정말 다릅니다. 몇 가지만 예를 들면:

| 형식 | 어떻게 쓰나 |
|---|---|
| 한 장면으로 시작하기 | 시간·장소·눈앞의 광경부터. 설명은 그다음 |
| 흔한 오해를 바로잡기 | 외국인이 믿는 것 하나를 적고, 왜 틀렸는지 |
| 왜를 세 번 묻기 | 현상 → 왜? → 왜? → 왜? 세 번째 답이 본문 |
| 짧은 대화로 보여주기 | 실제 대화 두세 줄을 적고 그 아래에서 풀기 |
| 사물 하나를 들여다보기 | 영수증·손잡이·간판 하나로 글 전체를 끌고 가기 |
| 뜯어보기 | 영수증의 각 줄, 상의 각 그릇을 하나씩 |
| 먼저 온 사람이 쓰는 편지 | 여섯 달 먼저 온 사람이 말하듯 |
| 공간을 따라 걷기 | 문 앞, 들어서면, 왼쪽에, 안쪽에 |

**고르는 방식은 무작위가 아닙니다.** 무작위로 뽑으면 하루 일곱 편 중
둘이 같은 형식일 확률이 절반쯤 됩니다. 대신 날짜와 주제 번호로 순서를
밟아 나갑니다. 그래서:

- 같은 날 8편이 **모두 다른 형식**
- 같은 주제가 같은 형식을 다시 만나기까지 **15일**
- 서른 가지가 **전부 쓰임** — 안 쓰이는 형식이 없음
- 날짜만 알면 어떤 형식이었는지 **다시 알 수 있음** (글이 이상하게
  나왔을 때 어떤 형식 탓인지 추적 가능)

실행 로그에 `형식: 짧은 대화로 보여주기` 처럼 찍힙니다.

### 글 아래 낱말 목록

이제 로봇이 글을 쓸 때 **낱말 목록(Words to know)도 같이 만듭니다.**
중급 학습자가 걸릴 만한 낱말 다섯 개를, 8개 언어 각각의 설명과 함께.
예전에는 관리자가 버튼을 눌러야 생겼습니다.

### 사진은 반드시 붙습니다

예전에는 **어울리는 사진이 없으면 그냥 없이** 저장했습니다. 비자 서류
이야기 밑에 해변 사진이 붙는 것보다는 낫다는 이유였는데, 실제로는 사진
없는 글이 미완성처럼 보였습니다.

이제 **다섯 번** 찾습니다:

1. 모델이 만든 가장 구체적인 검색어
2. 한 단계 넓힌 검색어
3. 글의 큰 주제를 나타내는 검색어
4. **그 주제(카테고리)의 기본 검색어** — 예: Style이면 "Korean skincare products"
5. 주제의 두 번째 기본 검색어

1~3번에서는 예전과 똑같이 **까다롭게** 고릅니다. 어울리지 않으면 버립니다.
다만 **마지막 검색어에서는 나온 것 중 가장 나은 것을 받습니다.** 그
검색어가 "그 글이 속한 주제" 자체라서 빗나갈 수가 없기 때문입니다.
한국 거리 이야기 밑의 한국 거리 사진은 어긋난 게 아닙니다.

> 사진 서비스 자체가 죽어 있으면(키 거부, 사용량 초과) 사진 없이
> 저장됩니다. 없는 사진을 만들어낼 수는 없으니까요. 로그에 이유가
> 남습니다.

### 하루에 한 번, 빠진 것만

새벽 작업은 **그날 아직 없는 주제만** 씁니다.

- 아무것도 없는 날 → 7편 전부
- 3편만 있는 날 → 빠진 4편만 (있는 3편은 그대로 둡니다)
- 7편 다 있는 날 → 아무것도 안 하고 끝

그래서 **같은 날 몇 번을 돌려도 글이 두 번 생기지 않고**, 중간에 멈춘
날도 다시 돌리면 채워집니다. 예전에는 "오늘은 이미 돌았다"며 그냥
끝내서, 한 편만 들어간 날이 영영 한 편으로 남았습니다.

> 실제로 2026-09-23 이 그랬습니다. 전날 밤 시험 실행이 여행 글 한 편을
> 쓰면서 그날을 차지했고, 새벽 5시 30분 작업은 "이미 돌았다"며 끝냈습니다.
> 지금은 이런 날 다시 돌리면 나머지 6편이 채워집니다.

### 커뮤니티 — 세 갈래

커뮤니티도 블로그와 같은 모양이 되었습니다. 맨 위에 **큰 상자 네 개** —
왼쪽이 [전체 글], 그 옆으로 세 갈래. 상자를 누르면 그 갈래의 글만
보이고, 페이지가 새로 뜨지는 않습니다.

| 이름 | 들어가는 내용 |
|------|---------------|
| Ask & Help (질문과 도움) | 한국어·한국 생활 질문과 그 답변 |
| Share & Talk (경험과 이야기) | 경험담, 의견, 한국에서의 일상 |
| Meet & Connect (만남과 교류) | 자기소개, 친구·스터디 파트너 찾기 |

셋인 이유는 사람들이 여기 오는 목적이 셋이기 때문입니다 — 묻거나,
말하거나, 만나거나. 넷째를 두면 셋과 구분이 안 되고, 빈 갈래는 죽은
페이지처럼 보입니다.

글을 쓸 때 **어디에 올릴지 고르는 칸**이 생겼습니다. 지금 보고 계신
갈래가 기본값입니다 — [질문과 도움]을 열어 두고 글쓰기를 누르셨다면
십중팔구 질문이실 테니까요.

**답글은 갈래를 고르지 않습니다.** 답글은 그 글타래의 일부이지 따로 쓴
글이 아니라서, 원글의 갈래를 그대로 따라갑니다.

> 예전에 쓰인 글(갈래가 없는 글)은 **경험과 이야기**로 들어갑니다.
> 데이터베이스 기본값도 같아서, SQL을 아직 안 돌리셨더라도 글이
> 사라지지는 않습니다.

주소는 `/community/ask` 처럼 됩니다. 링크를 보내면 그 갈래가 열린
채로 보입니다.

### 커뮤니티 — 여덟 언어가 한자리에

커뮤니티에서는 **쓰는 언어와 읽는 언어가 따로**입니다.

- **쓸 때**: 베트남 사람은 베트남어로, 일본 사람은 일본어로 그냥 씁니다.
  글쓰기 창에 **[작성 언어]** 칸이 생겼고, 타이핑하는 동안 어떤 언어인지
  알아서 채워집니다. 틀렸으면 그 자리에서 바꾸면 되고, 한 번 바꾸면
  다시는 건드리지 않습니다.
- **읽을 때**: 글마다 `VI · Tiếng Việt` 같은 **작성 언어 표시**가 붙고,
  내가 보고 있는 언어와 다르면 **[한국어로 보기]** 버튼이 나옵니다.
  누르면 그 글과 **그 아래 답글까지 한 번에** 내 언어로 바뀝니다.

번역된 글에는 **반드시** `자동 번역됨 · 원문: Tiếng Việt` 이 붙고,
**[원문 보기]** 가 한 번 누르면 되는 자리에 있습니다. 기계가 옮긴 말을
그 사람이 한 말처럼 보이게 두지 않는다는 뜻이고, 한국어 예문이나
인용처럼 **글자 하나가 중요한 대목**은 언제든 원문으로 확인할 수
있습니다.

몇 가지 정해 둔 것:

- **글은 하나입니다.** 언어별 게시판도, 번역본이라는 이름의 새 글도
  만들지 않습니다. 주소도 좋아요도 답글도 전부 원글 하나에 붙습니다.
- **미리 여덟 개를 번역하지 않습니다.** 누가 실제로 그 언어로 읽겠다고
  눌렀을 때만 번역합니다.
- **한 번 번역한 것은 기억합니다.** 같은 글을 같은 언어로 읽는 두 번째
  사람은 번역 비용이 0원이고, 기다리지도 않습니다.
- **글을 고치면 옛 번역은 버려집니다.** 바뀐 본문 아래에 예전 번역이
  남아 있는 일은 없습니다.
- 목록 위에 **[언어]** 고르는 칸이 생겼습니다. 기본은 *모든 언어*이고,
  갈래(질문·경험·만남)와 **같이** 걸립니다. 언어가 하나뿐일 때는 칸이
  아예 안 보입니다.
- 검색은 **원문을 찾습니다.** 예전 그대로입니다.

> 예전에 쓰인 글에는 작성 언어가 없습니다. 표시가 안 붙고, 대신
> 번역할 때 기계가 알아서 무슨 언어인지 알아냅니다.

#### 돈이 얼마나 드는가, 어디서 보는가

번역 회사는 **블로그에 쓰는 것과 같은 키 하나**를 그대로 씁니다
(위의 *블로그 자동 번역* 표). 커뮤니티용으로 따로 가입하실 것은
없습니다.

쓴 만큼은 그 회사 화면에서 봅니다 — Anthropic은 console.anthropic.com →
Usage, OpenAI는 platform.openai.com → Usage, Google은 aistudio.google.com,
DeepL은 계정 화면입니다. 한도를 정해 두고 싶으시면 같은 화면에
월 상한(spend limit)을 거시면 됩니다.

늘어날 수 있는 최대치는 **글 수 × 언어 수**까지이고, 한 번 번역한 것은
다시 청구되지 않아서 시간이 갈수록 줄어듭니다. 글을 읽는 사람이 많아진다고
비용이 따라 늘지는 않습니다.

#### 번역을 기억하게 하려면 (선택, 한 번만)

이걸 안 하셔도 **번역은 됩니다.** 다만 매번 다시 번역해서 돈이 더 듭니다.

1. 아무 긴 임의 문자열을 하나 정합니다 (예: 무작위 40자).
2. Vercel → Settings → Environment Variables → `TRANSLATE_CACHE_SECRET`
   에 그 문자열을 넣습니다.
3. Supabase → SQL Editor 에서 **같은 문자열**로:

   ```sql
   insert into public.app_secrets (name, value)
   values ('translate_cache', '여기에-그-문자열')
   on conflict (name) do update set value = excluded.value;
   ```

이 열쇠로 할 수 있는 일은 **번역을 저장하는 것 하나뿐**입니다. 데이터베이스
전체를 읽는 `service_role` 키는 여기에 쓰지 않았습니다 — 방문자가 닿는
자리에 둘 물건이 아니기 때문입니다.

### 글 안의 소제목

본문의 `## 소제목` 은 **형광펜으로 칠한 것처럼** 표시됩니다. `##`
기호는 화면에 보이지 않습니다. 번역을 나란히 보는 화면에서도 마찬가지로
원문과 번역 양쪽에 같은 형광펜이 칠해집니다.

> 소제목 바로 아랫줄에 빈 줄 없이 본문을 쓰셔도 됩니다. 예전에는 그럴
> 때 `##` 이 그대로 보였는데, 고쳤습니다.

### 블로그 요약·분류·태그 자동 생성

본문을 다 쓰고 잠시 멈추면 **요약·분류·태그 세 가지가 한 번에**
자동으로 채워집니다 (글을 쓴 언어로).

- 어느 칸이든 **직접 손대시면 그 칸은 더 이상 건드리지 않습니다**
- 셋 다 새로 받고 싶으면 태그 옆의 **[본문 다시 읽기]** 버튼
- 본문이 너무 짧으면(80자 미만) 아무 요청도 하지 않습니다
- 요청은 **한 번**입니다 — 세 가지를 따로 물으면 서로 다른 얘기를
  할 수 있어서 한 번에 묶었습니다

**태그는 독자 언어로 보입니다.** 한글로 `유학`이라고 달아도, 중국어로
읽는 사람에게는 중국어 태그가 보입니다. 다만 **누르면 원래 한글
태그로 검색**되므로 글이 제대로 찾아집니다. (번역할 때 같이 만들어
지니, 본문을 고치면 [다시 번역]을 한 번 눌러 주세요.)

### 블로그 주제 — 일곱 가지

블로그는 **다른 나라에서 한국에 온 사람**을 위한 글로 정리되어 있습니다.
닷새 머무는 여행자, D-2 유학생, 육 년째 사는 가족 — 이들이 실제로
찾는 방식대로 나눴습니다.

| 메뉴 이름 | 들어가는 내용 |
|-----------|---------------|
| Travel (여행) | 앱, 교통, 돈, 안전과 예절 |
| Dining (음식) | 주문과 식사법, 길거리·편의점, 식단 |
| Style (스타일) | 스킨케어, 병원, 브랜드, 쇼핑 |
| Explore (탐방) | 동네, K-팝·드라마 촬영지, 당일치기 |
| Campus & Life (유학·생활) | 비자, 집, 병원, 다문화 지원 |
| Career (커리어) | 아르바이트, 취업, 이력서, 인턴 |
| Korean Language Tips (한국어 공부 팁) | 바로 쓰는 표현, 자주 하는 실수, 생활 한국어 |
| ETC (기타) | 문화 차이, 외국인 관련 소식, 그 밖의 이야기 |

**Blog과 Community가 같은 모양입니다.** 맨 위에 넓은 [All posts] 버튼
하나, 얇은 구분선, 그 아래 BROWSE BY TOPIC과 카드들.

[All posts]는 **아홉 번째 카드가 아닙니다.** 주제에서 빠져나오는
버튼이라 더 크고, 혼자 있고, 전체를 보는 중에는 초록색으로 채워지고
주제를 고른 중에는 테두리만 남습니다. 언어 선택은 그 옆에 따로
띄워뒀습니다 — 안 그러면 버튼이 화면 가운데가 아니라 "언어 선택을 빼고
남은 자리"의 가운데에 놓입니다.

카드에는 아이콘, 이름, 설명 한 줄뿐입니다. **글 개수는 뺐습니다** —
아직 글이 없는 주제가 "0개"로 보이면 가볼 곳이 아니라 빈 곳처럼
읽힙니다. 개수는 목록 제목 아래에 있습니다. 설명은 잘리지 않고, 카드가
늘어나고 줄 전체가 같이 늘어납니다.

데스크톱 4열, 태블릿 2열, 휴대폰 1열입니다.

세부 주제(24개)는 만들었다가 **다시 뺐습니다** — 메뉴가 벽처럼
느껴졌고, 아래 줄의 주제를 가렸습니다. 이름 옆의 한글 글자(길·맛·멋…)도
같은 이유로 없앴습니다. 상단 메뉴의 **Blog에 달려 있던 주제 목록도
없앴습니다** — 화면의 큰 상자가 이미 그 역할을 하는데, 같은 이름 일곱
개를 헤더에 한 번 더 두면 읽을 것만 늘어납니다.

일곱 번째 주제는 **Community에서 ETC로 이름을 바꿨습니다.** 상단 메뉴의
방명록이 Community가 되었고, 한 사이트에 Community가 둘일 수는
없습니다. 주소도 `/blog/etc`로 바뀝니다.

글마다 **누구를 위한 글인지**를 색 배지로 표시합니다 — 여행자용(초록)
· 유학생용(파랑) · 거주자용(보라). 거르는 기능은 아니고, 남의 글을
열어보지 않도록 알려 주는 표시입니다.

글을 쓰실 때는 **본문만 채우시면** 주제·독자·요약·태그가 한 번에
정해집니다. 직접 바꾸신 칸은 다시 건드리지 않습니다.

글이 일곱 편을 넘으면 상자 아래에 **먼저 읽어 볼 글** 세 편이 나옵니다.

주소도 읽기 쉬운 형태입니다 — `durukorean.com/blog/travel`,
`durukorean.com/blog/post/제목`. 그대로 복사해서 보내셔도 됩니다.

> 주제를 새로 만들거나 이름을 바꾸고 싶으시면 AI에게 말씀하세요.
> 여덟 개 언어 번역과 데이터베이스 설정까지 함께 손대야 합니다.

### 매일 아침 초안 7편 (자동)

매일 새벽 5시 30분(서울)에 **7개 주제마다 한 편씩, 모두 7편**이
**임시저장** 상태로 만들어집니다. **자동으로 공개되지는 않습니다** —
대표님이 읽고 승인하신 것만 나갑니다.

아침 7시 전에 `blog.html`에 들어가시면 7편이 기다리고 있습니다.

**글이 만들어지는 순서**가 좀 특이한데, 이유가 있습니다.

1. **주제 먼저** — 후보 4개를 만들고 점수를 매겨 하나를 고릅니다.
   이때 이번 달 한국에서 일어나는 일(추석, 단풍, 김장…)과 외국인이
   자주 하는 질문, 그리고 **이미 쓴 글 목록**을 함께 봅니다.
2. **본문** — 500~800자, 소제목 2~3개.
3. **제목은 맨 마지막** — 본문을 다 쓰고 나서 후보 5개를 만들고
   그중 하나를 고릅니다. 제목을 먼저 정하면 제목에 맞춰 본문을
   억지로 채우게 되기 때문입니다.

**검사를 통과해야 저장됩니다.** 글자 수, 제목 길이, 태그 개수,
기존 글과 제목·주소 중복, "충격/99%가 모르는" 같은 낚시 표현,
"안녕하세요 오늘은~"으로 시작하는 상투적인 도입 — 전부 **세어 보고
맞춰 봅니다.** AI에게 "잘 썼니?"라고 묻지 않습니다. 걸리면 고치라고
돌려보내고, 두 번 해도 안 되면 **저장하지 않습니다.**

글을 열어 **[수정]**을 누르시면 제목 아래에 **함께 만들어진 제목 후보**
가 보입니다. 마음에 드는 걸 누르면 제목이 바뀝니다.

**사진도 함께 붙습니다.** AI가 그린 그림이 아니라 **실제 사진**입니다 —
한국 편의점이 어떻게 생겼는지 알려주는 글에 AI가 그린 가짜 편의점
사진을 붙이면 간판의 한글도 뭉개지고 글을 믿기 어려워집니다.

Unsplash와 Pexels에서 가져옵니다. 둘 다 **무료이고 상업적 이용이
허용**된 사진입니다. 글 내용으로 영어 검색어를 만들어 찾은 다음,
**"이 글에 정말 어울리는가"를 한 번 더 확인**합니다. 어울리는 게 없으면
**사진 없이 저장**합니다 — 비자 서류 이야기에 해변 사진을 붙이는
것보다 없는 게 낫습니다.

촬영자 이름과 링크가 사진 아래에 표시됩니다. 두 곳 다 법적으로는
표기 의무가 없지만, API 이용약관이 요청하는 사항이고 예의이기도
합니다.

> **출시 전에 두 곳의 라이선스를 한 번 직접 읽어 보시길 권합니다** —
> unsplash.com/license, pexels.com/license. 제 작업 환경에서는 외부
> 접속이 막혀 있어 현재 약관 원문을 확인하지 못했습니다.

사진이 마음에 안 들면 **글 수정 화면의 [사진] 칸**에서 영어로 검색해
다른 것으로 바꾸거나, **[빼기]**로 없앨 수 있습니다.

> **아직 안 되는 것**: Reddit이나 구글 실시간 검색은 못 봅니다
> (계절 달력과 "아직 안 쓴 주제" 분석으로 대신합니다). 나중에
> 붙일 수 있습니다.

### 자동 글쓰기를 켜려면 (한 번만)

**1. 로봇용 계정 만들기**

Supabase → Authentication → Users → **Add user**
- 이메일: 예) `bot@durukorean.com`
- 비밀번호: 길게, 아무거나. 적어 두세요.
- **Auto Confirm User** 켜기

**2. 그 계정을 관리자로 등록**

Supabase → SQL Editor에서 실행 (이메일은 위에서 만든 것으로):

```sql
insert into public.admin_users (user_id)
select id from auth.users where email = 'bot@durukorean.com'
on conflict do nothing;
```

**3. 사진 열쇠 받기 (무료)**

<https://unsplash.com/developers> → 가입 → New Application →
약관 동의 → 이름은 아무거나 (예: Duru Korean Blog) →
**Access Key**를 복사해 두세요.

> Pexels(<https://www.pexels.com/api/>)도 됩니다. 가입 즉시 키가
> 나오고 사용량이 넉넉합니다. 둘 중 하나만 있으면 됩니다.
> 사진 키가 없으면 **글만 만들어지고 사진 자리는 비어 있습니다.**

**4. GitHub에 열쇠 넣기**

GitHub 저장소 → Settings → Secrets and variables → **Actions** →
New repository secret, 세 개:

| 이름 | 값 |
|---|---|
| `DURU_BOT_EMAIL` | `bot@durukorean.com` |
| `DURU_BOT_PASSWORD` | 1번에서 정한 비밀번호 |
| `OPENAI_API_KEY` | 가지고 계신 OpenAI 키 |
| `UNSPLASH_ACCESS_KEY` | 사진용. unsplash.com/developers 에서 무료 발급 |

**5. 먼저 연습 삼아 돌려보기**

GitHub → **Actions** 탭 → *Daily blog drafts* → **Run workflow** →
`dry_run`을 **true**로, `only`에 `travel` → 실행.

아무것도 저장하지 않고 "이런 글을 쓸 참이었다"만 보여줍니다.
결과가 괜찮으면 `dry_run`을 끄고 한 번 더 돌려 실제로 저장해 보세요.

> **왜 service_role 키를 안 쓰나**: 그 키는 모든 보안 규칙을
> 건너뜁니다. 대신 **평범한 관리자 계정**으로 로그인하게 했습니다.
> 혹시 GitHub 비밀이 새더라도 새는 건 "블로그 초안을 쓸 수 있는
> 계정" 하나지, 데이터베이스 전체가 아닙니다.

**비용**: 글 한 편에 3~5번 정도 AI를 부릅니다. 하루 7편이면
20~35번. 작은 모델이면 하루 몇십 원 수준이지만, **첫 주에 OpenAI
사용량 화면을 한 번 확인해 주세요.** 예상과 다르면 모델을 바꾸면
됩니다 (GitHub → Settings → Variables → `TRANSLATE_MODEL`).

**멈추려면**: GitHub → Actions → *Daily blog drafts* → 오른쪽
**⋯ → Disable workflow**.

### 글에 찍히는 날짜

글에 보이는 날짜는 **초안을 쓴 날**입니다. 며칠 뒤에 승인하셔도
날짜는 움직이지 않습니다.

> 9월 22일에 초안 → 9월 25일에 승인 → 블로그에는 **9월 22일**로 표시

다른 날짜로 바꾸고 싶으시면 글 편집 화면의 **[게시 날짜로 표시]** 칸을
직접 고치시면 됩니다. 직접 고친 날짜는 그대로 유지됩니다.

목록 순서도 이 날짜 기준입니다 — 최근 날짜가 위, 같은 날이면 나중에
쓴 글이 위.

### 로그인 방법

이메일 외에 **Google · Facebook · X · 카카오톡**으로 로그인할 수
있습니다. 다만 각각을 **Supabase 대시보드에서 켜야** 실제로 작동합니다
(Authentication → Providers). 켜지 않은 것은 버튼을 눌러도
*"이 로그인 서비스는 아직 켜져 있지 않습니다"* 라고 답합니다.

> **네이버는 넣지 못했습니다.** Supabase가 네이버 로그인을 지원하지
> 않습니다. 억지로 만들려면 모든 보안 규칙을 건너뛰는 최고 권한 키를
> 웹 요청 경로에 두어야 해서, 그 위험이 네이버가 빠진 아쉬움보다
> 큽니다. Supabase가 지원하는 날 한 줄만 추가하면 됩니다.

### 블로그 하트·댓글

| | 로그인 안 함 | 로그인 함 |
|---|---|---|
| 하트 누르기 | ⭕ | ⭕ |
| 하트 취소 | ❌ | ⭕ (본인 것만) |
| 댓글·답글 쓰기 | ❌ | ⭕ |

로그인 없이 누른 하트는 **취소할 수 없습니다.** 브라우저에 저장된
임시 ID로는 "이 사람이 이미 눌렀다"까지만 믿을 수 있고, "취소하고
싶다"까지 믿으면 남의 하트를 지울 수 있는 구멍이 생기기 때문입니다.
누른 뒤에는 하트가 채워진 채로 멈추고 *"취소하려면 로그인하세요"*
라고 알려줍니다.

댓글은 로그인한 계정 이름으로 자동 작성됩니다. 이름을 직접 입력하는
칸이 없어서 남의 이름을 쓸 수 없습니다.

- 대표님이 로그인해 계시면 **모든 댓글에 [삭제]** 버튼이 보입니다
- 댓글을 지우면 **그 아래 달린 답글도 함께** 사라집니다
  (지울 때 몇 개가 같이 지워지는지 물어봅니다)
- 쓴 사람 본인도 자기 댓글은 지울 수 있습니다

### 학습자 이야기 관리
`www.durukorean.com/stories.html`

- 학생이 쓴 글은 **즉시 공개**됨
- 문제 있는 글은 대표님이 **삭제** 가능

### 이것들은 AI에게 요청해야 합니다
디자인 변경, 새 기능, 버그 수정, 문구 일괄 수정, 새 페이지 추가.

---

## 4. 구현 완료 현황

| 기능 | 상태 | 실제 확인 |
|---|---|---|
| 도메인 + HTTPS | 완료 | ✅ |
| 5개 언어 (영·베·한·일·중) | 완료 | ✅ |
| 이메일 회원가입·로그인 | 완료 | ✅ |
| 구글 로그인 | 완료 | ✅ |
| 관리자 권한 (RLS) | 완료 | ✅ |
| 자료 파일 업로드·삭제 | 완료 | ✅ |
| 개인정보처리방침·이용약관 | 완료 | ✅ |
| 블로그 쓰기/수정/삭제 | 완료 | ⏳ 첫 글 미작성 |
| 블로그 일곱 주제·독자 배지 | 완료 | ⏳ SQL 실행 필요 (7-A) |
| 게시 날짜 = 초안 쓴 날 | 완료 | ⏳ SQL 실행 필요 (7-A) |
| 매일 자동 글 생성 (초안 7편) | 완료 | ⏳ 로봇 계정·GitHub 비밀 설정 필요 |
| 자동 생성 글에 사진 (Unsplash·Pexels) | 완료 | ⏳ 사진 키 발급 필요 |
| 학습자 이야기 | 완료 | ⏳ 첫 글 미작성 |

"실제 확인"은 **실제 사이트에서 대표님이 직접 해보신 것**을 뜻합니다.
AI 작업 환경은 외부 접속이 차단되어 있어 로컬 테스트만 가능합니다.

---

## 5. 지켜야 할 규칙

### 보안 — 반드시

1. **Supabase `service_role` 키는 절대 저장소에 넣지 않습니다.**
   RLS를 무시하는 키라서, 노출되면 모든 데이터가 열립니다.
   서버에서만 써야 하고 이 사이트에는 서버가 없습니다.

2. **`anon` / `publishable` 키는 공개해도 됩니다.** 설계상 브라우저에
   노출되는 공개 키입니다. 실제 보호는 **RLS 정책**이 합니다.

3. **권한 검사는 항상 데이터베이스(RLS)에서 합니다.** 화면에서 버튼을
   숨기는 건 편의일 뿐 보호가 아닙니다. 관리자 여부는 `admin_users`
   테이블로만 판단합니다.

4. **학생이 쓴 글은 HTML로 해석하지 않습니다.** 일반 텍스트로만
   처리합니다. `js/blog.js`·`js/stories.js`의 `escapeHTML`을 없애거나
   `innerHTML`에 원본을 넣으면, 한 학생이 다른 학생 브라우저에서
   코드를 실행할 수 있게 됩니다.

5. **비밀번호·키가 보이는 화면은 캡처하지 않습니다.** 노출되면 교체.

### 보고 원칙

- 확인한 것과 확인 못 한 것을 **반드시 구분해서** 말합니다.
- "100% 안전" 같은 표현은 쓰지 않습니다.
- 실패하면 실패했다고 말합니다.

---

## 6. 오늘 겪은 함정 (다시 만나면 바로 알아보도록)

### Supabase

| 증상 | 진짜 원인 |
|---|---|
| 회원가입이 조용히 안 됨 | `Confirm email`이 켜져 있고 Site URL이 `localhost:3000` → 확인 메일 링크가 죽어 있음. 무료 플랜 메일은 소유자에게만 가는 경우가 많음 |
| 업로드 시 `Bucket not found` | Storage 버킷이 없음. **파일 목록 조회는 버킷 존재를 확인하지 못함** (정책만 있으면 빈 배열이 정상 반환됨) |
| 진단 4번 `HTTP 401` | `sb_publishable_` 키는 JWT가 아님. `Authorization: Bearer`로 보내면 401. `apikey` 헤더만 써야 함 |
| `schema.sql` 재실행 실패 | PostgreSQL에 `CREATE POLICY IF NOT EXISTS`가 없음 → `drop policy if exists` 를 앞에 붙여야 함 |

### 구글 로그인

| 증상 | 진짜 원인 |
|---|---|
| `앱 게시` 버튼이 회색 | 브랜딩 페이지 미완성. **어떤 칸이 빠졌는지 알려주지 않음** |
| `Unable to exchange external code` | Supabase의 Client Secret ≠ 구글의 Secret. 동의 화면이 떴다는 건 Client ID만 맞다는 뜻 |
| 돌아왔는데 로그아웃 상태 | PKCE 검증값은 **주소(origin)별로** 저장됨. apex에서 시작해 www로 돌아오면 실패. → `siteUrl` 설정으로 해결됨 |
| 대표님만 로그인됨 | 앱이 "테스트 중" 상태. 프로젝트 소유자는 항상 되므로 **본인 계정 테스트로는 확인 불가** |

### 기타

- **크롬 링크 클릭 시 다른 계정으로 열림** → 링크를 클릭하지 말고
  **복사해서 주소창에 붙여넣기**. 크롬 프로필이 여러 개일 때 발생.
- **수정했는데 화면이 그대로** → 브라우저 캐시. `Ctrl+Shift+R`.
  (`vercel.json`에서 재검증을 켜서 지금은 대부분 해결됨)
- **메뉴 항목을 추가하면 헤더가 깨짐** → 폭이 빠듯합니다. 현재 7개.
  추가 시 반드시 다시 측정해야 합니다 (아래 참조).

---

## 7. 헤더 폭 (건드리기 전에 읽으세요)

메뉴가 7개라 본문 폭(1180px)에 안 들어갑니다. 헤더 바만 **1560px**를
따로 씁니다.

1920px에서 실측한 필요 폭:

| 언어 | 필요 폭 |
|---|---|
| 영어 | 1308px |
| 베트남어 | 1477px |
| 일본어 | 1361px |
| 한국어 | 1186px |
| 중국어 | 1120px |

베트남어는 라벨이 길어 **1532px 아래에서 햄버거 메뉴로 접힙니다.**
나머지 언어는 1310px에서 접힙니다.

**이 숫자들은 측정값입니다.** 메뉴를 추가하거나 라벨을 바꾸면
다시 측정해야 합니다.

---

## 7-A. 지금 한 번 해 주셔야 하는 것 — SQL 실행

블로그를 일곱 주제로 바꾸면서 데이터베이스에 **주제 목록과 독자 구분**이
새로 필요해졌습니다. 이걸 실행하기 전까지는 글을 저장할 때 오류가
납니다.

1. Supabase → **SQL Editor** → New query
2. `supabase/schema.sql`의 **맨 아래 절들**을 붙여넣고 Run
   — 파일 전체를 다시 실행하셔도 됩니다. 몇 번을 실행해도 같은
   결과가 나오도록 만들어져 있습니다.
   아직 안 돌리신 것이 있다면 **31번**(커뮤니티 세 갈래)과
   **33번**(커뮤니티 다국어)입니다.
3. 확인: `supabase/verify.sql`을 실행하면 42줄이 전부 **OK**여야
   합니다.

> 33번을 돌리기 전에도 커뮤니티는 **예전 그대로 돌아갑니다** — 언어
> 표시도, 번역 버튼도, 언어 고르는 칸도 안 보일 뿐입니다. 먼저
> 올리고 나중에 SQL을 돌리셔도 중간에 페이지가 깨지지 않습니다.

기존 글은 **지워지지 않고 옮겨집니다** — 음식→음식, 문화·트렌드→탐방,
언어·기타→커뮤니티.

> GitHub 화면에서 복사하면 **보이는 데까지만 복사되는** 일이 있습니다.
> 중간에서 잘린 채 실행하면 `syntax error at or near ";"`가 납니다.
> 잘린 것 같으면 AI에게 다시 달라고 하세요.

---

## 8. 주소 모음

### 사이트
- 실제: <https://www.durukorean.com>
- 미리보기: <https://duru-korean-git-preview-cck6.vercel.app>
- 연결 진단: <https://www.durukorean.com/setup-check.html>
  (문제가 생기면 여기부터 — 10가지를 자동 점검합니다)

### 관리 화면
- GitHub: <https://github.com/cck0688-stack/duru-korean>
- Vercel: <https://vercel.com/cck6/duru-korean>
- Supabase: <https://supabase.com/dashboard/project/ejiwgvlinlffkyycuyym>
- 가비아 DNS: My가비아 → 서비스관리 → 도메인 → DNS 관리

### Supabase 자주 쓰는 화면
```
SQL 실행   /project/ejiwgvlinlffkyycuyym/sql/new
로그인 설정 /project/ejiwgvlinlffkyycuyym/auth/providers
주소 설정   /project/ejiwgvlinlffkyycuyym/auth/url-configuration
파일 저장소 /project/ejiwgvlinlffkyycuyym/storage/buckets
가입자 목록 /project/ejiwgvlinlffkyycuyym/auth/users
```

### DNS 설정값 (참고)
| 타입 | 호스트 | 값 |
|---|---|---|
| A | `@` | `216.198.79.1` |
| CNAME | `www` | `7b58ecfdee8f3dc3.vercel-dns-017.com.` |

---

## 9. 남은 일

### 보안 — 가급적 빨리
- [ ] **구글 콘솔에서 옛 Client Secret(`****rq_l`) 삭제**
      화면 캡처에 노출된 값입니다. 새 키로 정상 동작 중이므로
      지워도 로그인은 끊기지 않습니다.
      <https://console.cloud.google.com/auth/clients>

### 정리 — 편할 때
- [ ] Supabase Redirect URLs에서 `cck0688-stack.github.io` 줄 삭제
- [ ] GitHub Pages 끄기 (Settings → Pages → Source: None)
      현재 Vercel과 이중 배포 중이라 옛 주소가 아직 살아 있습니다

### 콘텐츠
- [ ] 블로그 첫 글 작성
- [ ] 학습자 이야기 첫 글 작성

### 나중에 생각해볼 것
- 판매를 시작하면 무료 요금제 약관 확인 필요 (Vercel Hobby는 상업적
  이용에 제한이 있습니다)
- 구글 동의 화면에 `...supabase.co` 대신 "Duru Korean"을 띄우려면
  구글 앱 인증 또는 Supabase 커스텀 도메인(유료)이 필요합니다
- 연습용 Supabase 프로젝트를 따로 두면 미리보기에서 마음 놓고
  테스트할 수 있습니다

---

## 10. 문제가 생기면

1. **`setup-check.html` 을 먼저 엽니다.** 10가지를 자동 점검하고,
   실패한 항목마다 해결 방법을 화면에 띄웁니다.
2. 화면에 뜬 **오류 문구를 그대로** 전달합니다. 오늘 대부분의 시간은
   "왜 안 되는지 화면이 말해주지 않아서" 썼습니다. 지금은 대부분의
   실패가 사유를 표시합니다.
3. 안 되는 화면을 **캡처**합니다. 단, 비밀번호·키가 보이면 가립니다.

---

## 11. 기술 문서

코드 구조, i18n 동작 방식, RLS 정책 설계, 배포 설정은 `README.md`에
영어로 정리되어 있습니다. AI가 작업할 때 참고하는 문서입니다.

데이터베이스 구조는 `supabase/schema.sql`에 주석과 함께 들어 있습니다.
**몇 번을 실행해도 안전합니다.**
